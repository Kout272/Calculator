# Use Cases — Multi‑Calculator

UC1: Вычисление выражения — ввод → '=' → safe_eval → результат/ошибка.
UC2: Построение графика — f(x) → построить → график/ошибка.
UC3: Конвертация валют — суммы/направления → расчёт → результат/офлайн.
UC4: Кредит/Ипотека — параметры → расчёт → платёж/график/валидация.
